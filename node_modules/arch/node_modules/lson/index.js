// Generated by LiveScript 1.3.1
(function(){
  var LiveScript, CSON, strip_trailing_semicolon, strip_parentheses, parse_lson;
  LiveScript = require('LiveScript');
  CSON = require('cson-safe');
  strip_trailing_semicolon = function(str){
    if (str[str.length - 1] === ';') {
      return str.slice(0, str.length - 1);
    }
    return str;
  };
  strip_parentheses = function(str){
    if (str[0] === '(' && str[str.length - 1] === ')') {
      return str.slice(1, str.length - 1);
    }
    return str;
  };
  parse_lson = function(str){
    var compiled;
    compiled = LiveScript.compile(str, {
      header: false,
      bare: true
    });
    return CSON.parse(strip_parentheses(strip_trailing_semicolon(compiled)));
  };
  module.exports.parse = function(str){
    return parse_lson(str);
  };
  module.exports.parseFile = function(filename){
    return parse_lson(require('fs').readFileSync(filename, 'utf8'));
  };
  module.exports.stringify = function(obj){
    return JSON.stringify(obj, null, 2);
  };
}).call(this);
